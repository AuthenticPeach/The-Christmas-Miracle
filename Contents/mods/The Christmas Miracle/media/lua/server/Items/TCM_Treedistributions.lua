require "Items/Distributions"
local TCM_distributionTable = {

	all = {
        TCMTree = {
            rolls = 1,
            items = {
				}
            },
    }
}
table.insert(Distributions, 2, TCM_distributionTable);